# List the start up tasks. Learn more https://www.gitpod.io/docs/config-start-tasks/
tasks:
  - command: |
      bash patch.sh
      gp ports await 23000 1>/dev/null && until sudo kill $(ps -fA | grep 'sh /ide/bin/gitpod-code' | head -n1 | awk '{print $2}'); do continue; done
